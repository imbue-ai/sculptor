/**
 * Extension id, doubling as this extension's query-key namespace on the shared
 * QueryClient (host keys live under "sculptor"; see extensions/README.md) and the
 * id of the registered panel.
 */
export const EXTENSION_ID = "linear-issue";

save-exact=true
# Use a flat, npm-like node_modules (no symlinked .pnpm store). Required for the
# Modal/offload sandbox, which has no symlink support, and the safe layout for
# electron-forge packaging.
node-linker=hoisted

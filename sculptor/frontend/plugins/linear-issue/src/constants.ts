/**
 * Plugin id, doubling as this plugin's query-key namespace on the shared
 * QueryClient (host keys live under "sculptor"; see plugins/README.md) and the
 * id of the registered panel.
 */
export const PLUGIN_ID = "linear-issue";

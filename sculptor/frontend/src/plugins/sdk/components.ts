/**
 * Sculptor first-party components plugins can use. The SDK runtime stub
 * (`public/plugin-runtime/sculptor-plugin-sdk.js`) reads these from the
 * `window.__SCULPTOR_HOST__.sdk` object the host populates at boot.
 */
export { PanelHeader } from "~/components/panels/PanelHeader.tsx";

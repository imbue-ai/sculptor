// Opaque string identifiers used across the state layer. `TaskID` keeps the
// wire's "task" vocabulary until the backend task→agent rename lands.
export type RequestID = string;
export type TaskID = string;
export type ProjectID = string;

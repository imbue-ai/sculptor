// Placeholder entrypoint for the Sculptor TypeScript backend. Later tasks
// replace this with the real Fastify server bootstrap.
export function main(): void {
  // eslint-disable-next-line no-console
  console.log("sculptor backend (placeholder)");
}

main();

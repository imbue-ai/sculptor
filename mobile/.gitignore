# Node
node_modules/

# Capacitor copies www into the native project and writes a generated config
# there on every `cap sync`; CI regenerates both, so keep them out of git.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
